package user

// Logic to manage user data (CRUD operations)

// RegisterUser registers a new user with basic details and returns the generated user.
func RegisterUser(email string, companyName string) (string, error)
